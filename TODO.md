# Linkina Markdown Editor TODO

## Workspace 交互式侧栏布局

### 第一阶段：布局状态和尺寸来源

- [x] 让 Workspace 成为 FileArea、DocumentArea 和 ExtendArea 宽度的唯一管理者。
- [x] 定义左右侧区互相独立的布局状态：
  - [x] `mode`：`expanded` 或 `collapsed`。
  - [x] `width`：当前展开宽度。
  - [x] `lastExpandedWidth`：收起前最后一次有效的展开宽度。
- [x] 定义统一的布局常量：
  - [x] FileArea 默认宽度 `260px`、最小展开宽度 `200px`、最大宽度 `420px`。
  - [x] ExtendArea 默认宽度 `320px`、最小展开宽度 `240px`、最大宽度 `480px`。
  - [x] 左右边栏收起宽度均为 `48px`。
  - [x] 分割条交互宽度为 `6px`，可见线条约为 `1px`。
- [x] 使用 CSS 变量向 Workspace Grid 传递左右侧区的当前宽度。
- [x] 将 Workspace 调整为“左侧区 / 左分割条 / 文档区 / 右分割条 / 右侧区”五列布局。
- [x] 移除 ExtendArea 自身的固定 `width: 400px`，避免子组件宽度与 Grid 列宽冲突。
- [x] 修复 Workspace 中无效的 `.work_space > *` 旧选择器。

### 第二阶段：完整区域和收起边栏

- [x] FileArea 展开时，在文件工具栏右侧提供“收起文件区”按钮。
- [x] ExtendArea 增加顶部工具栏，并在左侧提供“收起扩展区”按钮。
- [x] 左侧收起后显示 `48px` 宽边栏，暂时只保留“展开文件区”按钮。
- [x] 右侧收起后显示 `48px` 宽边栏，暂时只保留“展开扩展区”按钮。
- [x] 将 Navbar 中现有的左右侧区控制按钮移出，避免同一功能存在两套控制入口。
- [x] 根据展开/收起状态切换正确的 open/close 图标、`aria-label` 和 `title`。
- [x] 收起和展开时不销毁 FileArea 或 ExtendArea 的原有 DOM，保留列表滚动位置和组件内部状态。
- [x] 重新展开时恢复 `lastExpandedWidth`，而不是直接恢复默认宽度。

### 代码组织

- [x] 将收起边栏拆分为独立的 SidebarRail 组件。
- [x] 将侧栏配置、状态、渲染和收起/展开逻辑拆分到 `workspaceLayout.js`。
- [x] 将新建、删除、导入、导出和内容更新流程拆分到 `workspaceFileActions.js`。
- [x] Workspace 只保留组件组合、依赖注入和文件选择等跨组件协调。

### 第三阶段：分割条拖动和吸附收起

- [x] 为左右分割条增加 `role="separator"`、垂直方向语义和可聚焦能力。
- [x] 使用 Pointer Events 实现拖动，同时兼容鼠标、触控笔和触摸输入。
- [x] 拖动开始时记录指针位置和侧区起始宽度，并调用 `setPointerCapture()`。
- [x] 拖动左分割条时使用“起始宽度 + 水平位移”计算 FileArea 宽度。
- [x] 拖动右分割条时使用“起始宽度 - 水平位移”计算 ExtendArea 宽度。
- [x] 拖动过程中只更新内存状态和 CSS 变量，不写入 localStorage。
- [x] 拖动期间使用 `col-resize` 光标并禁止页面文字被意外选中。
- [x] 为接近收起阈值的侧区提供明确的吸附预览状态。
- [x] 在松开指针时决定最终状态，避免拖动过程中突然收起：
  - [x] FileArea 松手宽度低于约 `176px` 时吸附为左边栏，否则回弹到至少 `200px`。
  - [x] ExtendArea 松手宽度低于约 `216px` 时吸附为右边栏，否则回弹到至少 `240px`。
- [x] 允许从收起边栏的分割条向外拖动并直接展开侧区。
- [x] 双击分割条时将对应侧区恢复到默认宽度。

### 第四阶段：文档区保护和响应式约束

- [x] 为 DocumentArea 预留桌面端最小可用宽度，初始建议为 `480px`。
- [x] 根据窗口宽度、另一侧当前宽度和分割条宽度动态计算侧区最大可用宽度。
- [x] 防止左右侧区同时扩大后挤压或覆盖 DocumentArea。
- [x] 浏览器窗口尺寸变化时重新约束左右侧区宽度。
- [x] 空间不足时优先临时收起 ExtendArea，再根据需要临时收起 FileArea。
- [x] 响应式临时收起不能覆盖用户保存的展开宽度和偏好状态。
- [x] 移动端覆盖式抽屉布局作为后续阶段处理，不与第一轮桌面拖动布局同时实现。

### 第五阶段：持久化、键盘操作和回归检查

- [x] 使用独立的 localStorage 键保存布局偏好，不与 `MARKDOWN-FILES` 文件数据混合。
- [x] 只在拖动结束、点击收起/展开、恢复默认宽度或完成一次键盘调宽时保存布局。
- [x] 加载布局数据时校验状态和宽度，并根据当前窗口重新限制宽度。
- [x] 为分割条支持键盘调整：
  - [x] `ArrowLeft` / `ArrowRight` 每次调整约 `8px`。
  - [x] 配合 `Shift` 每次调整约 `24px`。
- [x] 动态更新分割条的 `aria-valuemin`、`aria-valuemax` 和 `aria-valuenow`。
- [x] 回归检查左侧拖动、右侧拖动、双侧同时展开、吸附收起、按钮收起和恢复宽度。
- [x] 回归检查收起/展开后文件选择、编辑、预览、导入、导出和删除功能不受影响。
- [x] 回归检查文件列表滚动位置不会因收起/展开而丢失。
- [x] 回归检查窄窗口下 DocumentArea 不被侧区覆盖，且各区域滚动互不干扰。
- [x] 完成后运行 `npm run build`。

## DocumentArea

- [x] 设计未选择文件时的空状态内容和布局。
- [x] 完善编辑/阅读模式按钮的选中、悬停、聚焦和禁用状态。
- [x] 完善编辑器输入区域的聚焦和禁用状态。
- [ ] 在亮色/暗色主题阶段统一正文、背景、边框、链接、引用和代码块的颜色变量。
- [x] 接入 Markdown 围栏代码的语法高亮，未知语言安全回退为 plaintext。
- [ ] 在主题阶段为代码高亮选择或设计对应的亮色/暗色主题。
- [ ] 使用源码行号锚点实现编辑模式和阅读模式的双向滚动定位同步（已讨论，暂缓）。
  - [ ] 在 Markdown 解析阶段获取块级 token 的源码起止行。
  - [ ] 为预览区块级元素写入 `data-source-start` 和 `data-source-end`。
  - [ ] 让 EditArea 暴露当前可视区域对应的源码行。
  - [ ] 让 PreviewArea 支持按源码锚点定位，并在相邻锚点之间插值。
  - [ ] 支持从 PreviewArea 反向映射到 EditArea。
  - [ ] 处理图片异步加载和超长代码块造成的布局偏移。
